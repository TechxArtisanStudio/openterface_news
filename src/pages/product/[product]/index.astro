---
import FeedLayout from '../../../layouts/FeedLayout.astro';
import { productFeedProps } from '../../../lib/feed-pages';
import { PRODUCTS, type ProductSlug } from '../../../config/products';

export async function getStaticPaths() {
  return PRODUCTS.filter((p) => p !== 'accessories').map((product) => ({
    params: { product },
  }));
}

const { product: productParam } = Astro.params;
const product = productParam as ProductSlug;
const props = await productFeedProps('en', product);
---

<FeedLayout
  locale={props.locale}
  title={props.title}
  description={props.description}
  articles={props.articles}
  basePath={props.basePath}
  showFilters={false}
/>
