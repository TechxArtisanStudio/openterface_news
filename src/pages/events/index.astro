---
import FeedLayout from '../../layouts/FeedLayout.astro';
import { eventsFeedProps } from '../../lib/feed-pages';

const props = await eventsFeedProps('en');
---

<FeedLayout
  locale={props.locale}
  title={props.title}
  description={props.description}
  articles={props.articles}
  basePath={props.basePath}
  showFilters={false}
/>
