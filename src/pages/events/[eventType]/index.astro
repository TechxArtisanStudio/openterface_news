---
import { homeFeedRedirect } from '../../../lib/feed-redirects';

export async function getStaticPaths() {
  return ['exhibition', 'contest'].map((eventType) => ({ params: { eventType } }));
}

return Astro.redirect(homeFeedRedirect('en'), 301);
---
